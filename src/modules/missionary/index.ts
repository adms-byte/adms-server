// @ts-ignore: resolve route module that is built/handled outside current tsconfig jsx settings
import missionaryRouter from "./routes";

const missionary = {
  router: missionaryRouter,
};
export default missionary;
