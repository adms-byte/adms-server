// @ts-ignore: resolve route module that is built/handled outside current tsconfig jsx settings
import childrensRouter from "./routes";

const childrens = {
  router: childrensRouter,
};
export default childrens;
